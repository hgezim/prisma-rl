{
  "name": "script",
  "license": "MIT",
  "devDependencies": {
    "@prisma/cli": "2.13.1",
    "ts-node": "9.1.1",
    "typescript": "4.1.3"
  },
  "scripts": {
    "dev": "ts-node ./script.ts",
    "prisma:generate": "prisma migrate dev --create-only --preview-feature",
    "prisma:run": "prisma migrate dev --preview-feature"
  },
  "dependencies": {
    "@prisma/client": "2.13.1"
  },
  "engines": {
    "node": ">=10.0.0"
  }
}
